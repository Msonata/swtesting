{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "c8 --src src --all --exclude src/.internal --exclude test --reporter lcov mocha",
    "testLocal": "c8 --src src --all --exclude src/.internal --exclude test mocha --reporter mochawesome"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "c8": "^7.12.0",
    "chai": "^4.3.7",
    "mochawesome": "^7.1.3",
    "mocha": "^10.1.0"
  }
}
